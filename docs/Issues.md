# tailwindcss 
- standard npx tailwindcss init -p does not work with its v4. 
- installed from 
- npm create vite@latest frontend -- --template react-ts, installs tailwindcss v4, so we need to install vite plugin of tailwindcss using
npm install tailwindcss @tailwindcss/vite